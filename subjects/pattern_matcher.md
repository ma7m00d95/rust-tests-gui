## Exercise 07: Going Higher Order

Create a "pattern matcher" system using closures.

Functions to implement:
- `anything()`, `exactly()`, `alphabetic()`, `ascii_digits()`, `min_size()`
- `maybe()`, `not()`, `or()`, `and()`, `chain()`

Use these to build an email matcher and other complex patterns.
