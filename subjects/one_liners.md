## Exercise 02: One Liners

Create two functions that process strings using iterators. **No `return` or `;` allowed!**

1. `sum_of_odds(s: &str) -> u32`: Sum of all odd numbers found in the string.
2. `create_pairs<T: FromStr>(s: &str) -> Vec<(&str, T)>`: Parse lines of "key: value".
