## Exercise 06: Big Add

Add two arbitrarily large numbers given as lists of decimal digits.

```rust
fn big_add(a: &[u8], b: &[u8]) -> Vec<u8>;
```

* Inputs must only contain digits `b'0'` to `b'9'`.
*Result must not have leading zeros.
